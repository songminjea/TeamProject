<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.message.Mapper.messageMapper">

	<!--  쪽지 작성 -->
	<insert id="insert">
		INSERT INTO MESSAGE(MESSAGE_NO, MESSAGE_SENDER, MESSAGE_RECEIVER, MESSAGE_SUBJECT, MESSAGE_CONTENT, MESSAGE_SENDTIME)
		VALUES(MESSAGE_seq.nextval, #{MESSAGE_SENDER}, #{MESSAGE_RECEIVER}, #{MESSAGE_SUBJECT}, #{MESSAGE_CONTENT}, NOW())
	</insert>
	<!-- 쪽지 내용 상세보기 조회 -->
	<select id="view" resultType="MessageVO">
		SELECT * FROM MESSAGE WHERE MESSAGE_NO = #{MESSAGE_NO}
	</select>
	<!-- 쪽지 삭제 -->
	<delete id="delete">
		DELETE FROM MESSAGE WHERE MESSAGE_NO = #{MESSAGE_NO}
	</delete>
	<!-- 쪽지 전체 목록 -->
	<select id="listAll" resultType="MessageVO">
		SELECT * FROM MESSAGE ORDER BY MESSAGE_SENDTIME DESC 
	</select>
</mapper>